{
  "name": "cursor-decision-notifier",
  "version": "1.0.0",
  "private": true,
  "description": "macOS browser notifications when Cursor needs a decision",
  "scripts": {
    "check": "node --check extension/background.js && node --check extension/content.js && node --check extension/decision-detector.js && node --check extension/popup.js",
    "test": "node --test tests/*.test.js"
  }
}
